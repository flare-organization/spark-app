{
  "name": "spark-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "update-code": "cd frontend && npm install && cd .. && mvn clean generate-sources",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "9.1.7",
    "lint-staged": "17.0.7"
  }
}
