{
  "name": "spark-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"backend,frontend\" --prefix-colors \"blue,green\" \"mvn spring-boot:run -pl backend -Dspring-boot.run.profiles=dev\" \"npm run dev --prefix frontend\"",
    "frontend": "npm run dev --prefix frontend",
    "backend": "mvn spring-boot:run -pl backend -Dspring-boot.run.profiles=dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}