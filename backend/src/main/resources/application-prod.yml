spark-variables:
  cors:
    allowed-origin: http://localhost:5173
  csrf:
    enabled: true
  seed-database: false

spring:
  docker:
    compose:
      lifecycle-management: none  # this is external on prod

logging:
  level:
    root: WARN
    com.flare.spark: INFO
