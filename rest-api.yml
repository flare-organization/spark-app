openapi: 3.0.3
info:
  title: Spark Bundle API
  version: 0.0.1

servers:
  - url: http://localhost:8080

paths:
  /api/v1/bundles:
    get:
      tags:
        - Bundles
      summary: Get a list of bundles
      operationId: getBundles
      parameters:
        - in: path
          name: page
          required: false
          schema:
            type: integer
            description: current page number
      responses:
        200:
          description: List of bundles
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Bundle'
    post:
      tags:
        - Bundles
      summary: Create a new bundle
      operationId: createBundle
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateBundle'
      responses:
        200:
          description: Returns the generated Bundle
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Bundle'

  /api/v1/bundles/search:
    get:
      tags:
        - Bundles
      summary: Get a filtered list of bundles
      operationId: searchBundles
      parameters:
        - in: query
          name: q
          required: true
          schema:
            type: string
          description: Search term matched against bundle name
        - in: query
          name: page
          required: false
          schema:
            type: integer
            description: current page number
      responses:
        200:
          description: List of bundles
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedSearchBundles'

  /api/v1/send-email:
    post:
      tags:
        - Email
      summary: Send an email
      operationId: send-email
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendEmail'
      responses:
        200:
          description: Empty response

  /api/v1/bundles/{name}:
    get:
      tags:
        - Bundles
      summary: Get a single bundle by name
      operationId: getBundleByName
      parameters:
        - in: path
          name: name
          required: true
          schema:
            type: string
          description: The unique name of the bundle
      responses:
        200:
          description: The requested bundle
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BundleDetail'
        404:
          description: No bundle exists with that name


components:
  schemas:
    SignUpCredentials:
      type: object
      required:
        - username
        - email
        - password
      properties:
        username:
          type: string
          pattern: '^[a-zA-Z0-9]+(?:-[a-zA-Z0-9]+)*$'
          minLength: 2
          maxLength: 64
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 8
          maxLength: 128

    LoginCredentials:
      type: object
      required:
        - username
        - password
      properties:
        username:
          type: string
          pattern: '^[a-zA-Z0-9]+(?:-[a-zA-Z0-9]+)*$'
          minLength: 2
          maxLength: 64
        password:
          type: string
          minLength: 8
          maxLength: 128

    Bundle:
      type: object
      required:
        - id
        - name
        - description
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string

    SlicedPagination:
      type: object
      required:
        - pageNumber
        - pageSize
        - isFirst
        - isLast
        - isEmpty
      properties:
        pageNumber:
          type: integer
        pageSize:
          type: integer
        isFirst:
          type: boolean
        isLast:
          type: boolean
        isEmpty:
          type: boolean

    PaginatedBundles:
      type: object
      required:
        - content
      properties:
        content:
          type: array
          items:
            $ref: '#/components/schemas/Bundle'
      allOf:
      - $ref: '#/components/schemas/SlicedPagination'

    CreateBundle:
      type: object
      required:
        - name
        - description
        - status
      properties:
        name:
          type: string
          pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'
          minLength: 1
          maxLength: 128
        description:
          type: string
          maxLength: 512
        status:
          type: string
          enum: [PUBLIC, PRIVATE]


    SearchBundle:
      type: object
      required:
        - name
        - description
        - status
      properties:
        name:
          type: string
        description:
          type: string
        status:
          type: string

    PaginatedSearchBundles:
      type: object
      required:
        - content
      properties:
        content:
          type: array
          items:
            $ref: '#/components/schemas/SearchBundle'
      allOf:
        - $ref: '#/components/schemas/SlicedPagination'

    BundleVersionSummary:
      type: object
      required:
        - version
        - publishedAt
      properties:
        version:
          type: string
        publishedAt:
          type: string
          format: date-time

    BundleVersionDetail:
      type: object
      required:
        - version
        - publishedAt
      properties:
        version:
          type: string
        readme:
          type: string
          nullable: true
        fileSize:
          type: integer
          format: int64
          nullable: true
        publishedAt:
          type: string
          format: date-time

    BundleDetail:
      type: object
      required:
        - id
        - name
        - description
        - status
        - tags
        - versions
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
        status:
          type: string
          enum: [PUBLIC, PRIVATE]
        tags:
          type: array
          items:
            type: string
        latestVersion:
          $ref: '#/components/schemas/BundleVersionDetail'
        versions:
          type: array
          items:
            $ref: '#/components/schemas/BundleVersionSummary'

    SendEmail:
      type: object
      required:
        - from
        - to
        - subject
        - body
      properties:
        from:
          type: string
          minLength: 1
          maxLength: 128
        to:
          type: string
          minLength: 1
          maxLength: 128
        subject:
          type: string
          minLength: 1
          maxLength: 128
        body:
          type: string
